apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: linkerd-config
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - cluster: local
        url: https://kubernetes.default.svc
  template:
    metadata:
      name: '{{cluster}}-linkerd-config'
    spec:
      project: core
      destination:
        namespace: linkerd
        server: '{{url}}'
      source:
        repoURL: https://github.com/tomaszwostal/devops-toys.git
        path: devops-app/linkerd-config/{{cluster}}
        targetRevision: v0.2
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
          allowEmpty: false
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=false
          - RespectIgnoreDifferences=true

