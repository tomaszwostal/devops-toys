apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: grafana-mimir-config
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - cluster: local
        url: https://kubernetes.default.svc
  template:
    metadata:
      name: '{{cluster}}-grafana-mimir-config'
    spec:
      project: core
      destination:
        namespace: mimir
        server: '{{url}}'
      source:
        repoURL: https://github.com/tomaszwostal/devops-toys.git
        path: devops-app/grafana-mimir-config/{{cluster}}
        targetRevision: v0.2
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
          allowEmpty: false
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=false
          - RespectIgnoreDifferences=true

