nameOverride: ""
## @param fullnameOverride String to fully override external-dns.fullname template
##
fullnameOverride: ""
resources:
  requests:
    cpu: 10m
    memory: 180Mi
sources:
  - service
  - ingress
provider: azure
azure:
  secretName: external-dns-hldx-io
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    labels:
        release: {{cluster}}-prometheus-stack